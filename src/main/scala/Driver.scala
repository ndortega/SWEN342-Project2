package main.scala

/**
  * At the beginning of the day, the system will initialize and turn on all of the scanners.
  *
  * At the end of the day, the system closes by turning off all the scanners, and sending prisoners
  * in jail to permanent detention facilities. As passengers and their bags clear through the system, it will shutdown, i.e. the queue must be empty and the body scanner must be ready before the body scanner is turned off.
  */
object Driver {

  def main(args: Array[String]): Unit = {

    println("hello world")
  }

}